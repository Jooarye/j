// main function
fn m(int a, str[] b) str {
  if a == len(b) {
    int i = 0;

    while i < len(b) {
      print(b[i]);
      i++; // random ik
    }
  } else {
    print("Invalid length");

    for i = 0; i < 12; i++ {
      print("i is", i);
    }
  }
}
